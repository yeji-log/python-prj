--- PRD & 계획 ---
## PRD

### 문제(병목) 한 문단
기존 파이썬 자료에서 개념 학습과 수준별 테스트가 동작될 수 있도록 하고 싶다.

### MVP — 12시까지 반드시 동작할 한 가지
파이썬 자료 내용을 넣으면 개념 학습과 수준별 테스트가 동작될 수 있도록.

### CONTEXT — AI에게 줄 맥락
2학년, 동아리학생 대상. 파이썬 활용.

### P1 — 오늘은 안 만들 것
개념 학습과 수준별(상,중,하) 테스트가 동작

### 스택·배포
next.js+firebase+vercel

## 구현 계획

### 사용자 시나리오
동아리 2학년 학생이 접속하면, 교사가 노션이나 코랩에 정리해둔 수업 자료를 복사해 붙여넣어 만든 파이썬 개념 목록이 화면에 나타난다. 학생은 목록에서 개념 하나(예: 반복문)를 골라 내용을 학습한다. 학습을 마치면 그 개념에 대한 난이도(상/중/하)를 선택해, 해당 개념만 다루는 간단한 코드 테스트를 진행한다. 결과에서 정답 개수를 확인하고, 필요하면 틀린 문제를 다시 보거나 같은 테스트를 재시작한 뒤, 개념 목록으로 돌아가 다른 개념을 이어서 학습하거나 접속을 종료한다.

### 화면 목록 + 텍스트 와이어프레임
① 자료 입력 화면
┌─────────────────────────────┐
│ 수업 자료로 개념 만들기        │
├─────────────────────────────┤
│ 노션·코랩 내용을 복사해        │
│ 아래에 붙여넣으세요            │
│ [ 자료 붙여넣기 텍스트박스   ] │
│ [                          ] │
│                              │
│      [ 개념 생성하기 ]        │
└─────────────────────────────┘
② 개념 목록 화면
┌─────────────────────────────┐
│ 개념 목록                     │
├─────────────────────────────┤
│ ○ 반복문(for)                │
│ ○ 조건문(if)                 │
│ ○ 리스트                     │
│ ○ 함수                       │
│                              │
│      [ 선택한 개념 학습 ]      │
└─────────────────────────────┘
③ 개념 학습 화면
┌─────────────────────────────┐
│ 반복문(for)                   │
├─────────────────────────────┤
│ 설명 텍스트 영역...           │
│                              │
│                              │
│        [ 테스트 시작 ]        │
└─────────────────────────────┘
④ 난이도 선택 화면
┌─────────────────────────────┐
│ '반복문' 테스트 난이도         │
├─────────────────────────────┤
│   [ 상 ]   [ 중 ]   [ 하 ]    │
│                              │
│        [ 시작하기 ]           │
└─────────────────────────────┘
⑤ 코드 테스트 화면
┌─────────────────────────────┐
│ 반복문 문제 2/5     [중]      │
├─────────────────────────────┤
│ Q. 아래 코드의 실행 결과는?    │
│ ┌─────────────────────┐     │
│ │ for i in range(5):  │     │
│ │     print(i)        │     │
│ └─────────────────────┘     │
│ ○ 0~4  ○ 1~5  ○ 0~5  ○ 오류 │
│        [ 제출 ]              │
└─────────────────────────────┘
⑥ 결과 화면
┌─────────────────────────────┐
│ '반복문' 결과                 │
├─────────────────────────────┤
│ 정답 4 / 5                   │
│ [ 틀린 문제 다시보기 ▸ ]      │
│                              │
│ [ 다시 풀기 ] [ 개념 목록 ]    │
└─────────────────────────────┘
다시 풀기 → 같은 개념·같은 난이도로 ⑤ 테스트 화면을 새 문제로 재시작
개념 목록 → ② 개념 목록 화면으로 이동
⑦ 오답 리뷰 화면
┌─────────────────────────────┐
│ 틀린 문제 다시보기   ◂ 결과   │
├─────────────────────────────┤
│ Q2. (내 답: X / 정답: O)     │
│ Q4. (내 답: X / 정답: O)     │
│                              │
│  [ 결과로 ]   [ 다시 풀기 ]   │
└─────────────────────────────┘
결과로 → ⑥ 결과 화면으로 복귀
다시 풀기 → ⑥과 동일하게 같은 개념·난이도 재시작

### 테스트 케이스 설계 (정상 2 + 실패 3)
정상 케이스 (2개)
노션·코랩에서 복사한 자료를 붙여넣고 "개념 생성하기"를 누르면 자료에서 추출된 개념 목록이 나타나야 한다.
개념 하나를 선택하고 난이도를 골라 테스트를 마치면, 그 개념에 대한 정답 개수와 틀린 문제가 결과 화면에 표시되어야 한다.
실패 케이스 (5개)
자료를 아무것도 입력하지 않고 "개념 생성하기"를 누르면 자료를 먼저 입력하라고 알려줘야 한다.
파이썬과 무관하거나 개념 추출이 어려운 텍스트(예: 코랩 실행 로그만 붙여넣은 경우)를 넣으면, 개념을 추출하지 못했다는 안내와 함께 자료를 다시 입력하도록 유도해야 한다.
특정 개념에 대해 문제가 하나도 생성되지 않은 채로 테스트를 시작하려 하면, 해당 개념은 테스트를 진행할 수 없다고 안내하고 다른 개념 선택으로 안내해야 한다.
테스트 중 새로고침하거나 뒤로가기를 하면 진행 중이던 답안·점수가 사라졌음을 알리고 해당 개념·난이도의 테스트를 처음부터 다시 시작할 수 있어야 한다. (같은 개념을 다시 선택해도 이전 회차 답안이 남지 않고 항상 새 문제로 초기화되어야 한다.)
문제 데이터 일부에 선택지나 정답이 누락·오류로 저장되어 있으면, 해당 문제 때문에 테스트 전체가 멈추거나 채점이 깨지지 않고 그 문제는 건너뛰거나 별도 표시로 처리해야 한다.